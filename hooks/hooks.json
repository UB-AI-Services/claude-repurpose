{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys; f=sys.argv[1]; exit(0) if not f.endswith('.md') or open(f).read().strip() else exit(0)\" \"$FILE_PATH\" 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
